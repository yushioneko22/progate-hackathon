# syntax=docker/dockerfile:1.7
FROM node:22-alpine
RUN corepack enable && corepack prepare pnpm@10.5.0 --activate
WORKDIR /repo
EXPOSE 3000
CMD ["sh", "-c", "pnpm install --frozen-lockfile && pnpm --filter @app/web dev"]
